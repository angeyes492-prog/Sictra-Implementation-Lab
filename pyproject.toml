[project]
name = "sictra-block1"
version = "0.1.0"
description = "Bounded Context-to-Reassessment execution slice for SICTrA Block 1"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"

