[project]
name = "sictra-block1"
version = "0.3.0"
description = "Bounded operational runtime for Telecare OS Block 1 Intelligence"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"

[tool.setuptools.package-data]
sictra_block1 = ["web/*.html", "web/*.css", "web/*.js"]

